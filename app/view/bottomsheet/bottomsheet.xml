<GridLayout rows="auto" verticalAlignment="bottom" height="300"  borderRadius="25 25 0 0" backgroundColor="Blue">
    <StackLayout padding="30">
        <Label text="Nueva nota" fontSize="24" fontWeight="bold" horizontalAlignment="center"/>
        <TextField hint="Title" text="{{ title }}" class="input-field"/>
        <TextView hint="Description" text="{{ description }}" maxLines="5" class="input-field"/>

        <FlexboxLayout justifyContent="space-around" style="margin-top: 10">
            <Button class="button" text="Cancelar" tap="{{ onCancelTap }}"/>
            <Button class="button" text="Añadir nota" tap="{{ onConfirmTap }}"/>
        </FlexboxLayout>
    </StackLayout>
</GridLayout>
